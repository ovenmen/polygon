import { API_URL } from "./constants";

export const apiUrls = {
    sigin: `${API_URL}/users/sigin`,
    articles: `${API_URL}/articles`,
    users: `${API_URL}/users`,
    uploads: `${API_URL}/upload/files`,
    media: `${API_URL}/media`
};
