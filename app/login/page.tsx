import React from 'react';
import SiginForm from '../components/Sigin-form/Sigin-form';

const LoginPage = (props) => (
    <section>
        <SiginForm />{console.log(props)}
    </section>

);
  
export default LoginPage;
